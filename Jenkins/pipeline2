pipeline {
    agent any

    environment {
        GIT_REPO = 'https://github.com/vishruthreddy/K8S.git'
        BRANCH = 'main'
        FILE_PATH = 'K8S/Ansible/apache.yml'
        KUBECONFIG = '/var/lib/jenkins/.kube/config' // This should exist and be readable
    }

    stages {
        stage('Clone Repository') {
            steps {
                git branch: "${BRANCH}", url: "${GIT_REPO}"
            }
        }

        stage('Check kubectl & Cluster Access') {
            steps {
                script {
                    sh 'which kubectl'
                    sh 'kubectl version --client'
                    sh 'kubectl config current-context'
                    sh 'kubectl get nodes'
                }
            }
        }

        stage('Apply Apache RBAC Configuration') {
            steps {
                script {
                    sh "kubectl apply -f ${FILE_PATH}"
                }
            }
        }
    }

    post {
        success {
            echo "✅ Apache RBAC configuration applied successfully."
        }
        failure {
            echo "❌ Failed to apply Apache RBAC configuration. Check kubectl and kubeconfig."
        }
    }
}
